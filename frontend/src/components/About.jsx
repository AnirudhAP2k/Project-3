import React from 'react';
// import noteContext from '../context/NoteContext';

function About() {
  // const a = useContext(noteContext);

  return (
    <div>
      This is about and is at class;
    </div>
  )
}

export default About
